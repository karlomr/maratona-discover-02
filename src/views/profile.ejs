<!DOCTYPE html>
<html lang="pt-BR">
  <head>
    <%- include('parts/page-head') %>
    <link rel="stylesheet" href="/styles/pages/profile.css" />
  </head>

  <body id="page-profile">
    <%- include('parts/page-header', { title: 'Add new job'}) %>

    <div class="container animate-up delay-2">
      <aside class="card">
        <img src="<%= profile.avatar %>" alt="<%= profile.name %>" />
        <h2><%=profile.name%></h2>
        <p>
          Your value hour is <br>
          <strong> <%= valueHourBrl %></strong><br>
          <strong> <%= valueHourUsa %></strong><br>
          <strong> <%= valueHourEur %></strong><br>
        </p>
        <button class="button green" form="form-profile" type="submit">
          Save data
        </button>
      </aside>
      <main>
        <form id="form-profile" method="POST" action="/profile">
          <fieldset>
            <legend>Profile Data</legend>
            <div class="separator light"></div>

            <div class="input-group">
              <div class="input-wrapper">
                <label for="name">Name</label>
                <input
                  type="text"
                  id="name"
                  name="name"
                  value="<%= profile.name %>"
                />
              </div>

              <div class="input-wrapper">
                <label for="avatar">Link photo</label>
                <input
                  placeholder="https://"
                  type="url"
                  id="avatar"
                  name="avatar"
                  value="<%= profile.avatar %>"
                />
              </div>
            </div>
          </fieldset>

          <fieldset>
            <legend>Planning</legend>
            <div class="separator light"></div>

            <div class="input-group">
              <div class="input-wrapper">
                <label for="monthly-budget">
                  How much <br/> 
                  do you want to receive per month ?
                </label>
                <input type="amount" step="0.1" id="monthly-budget"
                name="monthly-budget" placeholder="R$" value="<%=
                profile["monthly-budget"] %>"/>
              </div>

              <div class="input-wrapper">
                <label for="hours-per-day"
                  >How many hours <br/>
                  do you want to work?</label>
                <input type="number" id="hours-per-day" name="hours-per-day"
                value="<%= profile["hours-per-day"] %>"/>
              </div>
            </div>

            <div class="input-group">
              <div class="input-wrapper">
                <label for="days-per-week">
                  How many days of the week<br/>do you want to work?
                </label>
                <input type="number" id="days-per-week" name="days-per-week"
                value="<%= profile["days-per-week"] %>"/>
              </div>

              <div class="input-wrapper">
                <label for="vacation-per-year"
                  >How many weeks a year <br />
                   do you want to take a vacation?</label>
                <input type="number" id="vacation-per-year"
                name="vacation-per-year" value="<%= profile["vacation-per-year"]
                %>"/>
              </div>
            </div>
          </fieldset>
        </form>
      </main>
    </div>
    <!-- end container -->
  </body>
</html>
